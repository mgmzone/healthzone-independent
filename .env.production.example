# HealthZone Production Environment Variables
# Copy this to .env.production and fill in your actual values

# =============================================================================
# SUPABASE CONFIGURATION
# =============================================================================
# Get these from your Supabase project settings > API
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# =============================================================================
# APPLICATION CONFIGURATION  
# =============================================================================
# Your production domain (where users will access the app)
VITE_APP_URL=https://healthzone.yourdomain.com

# =============================================================================
# DEPLOYMENT CONFIGURATION
# =============================================================================
# Environment identifier
NODE_ENV=production

# =============================================================================
# NOTES
# =============================================================================
# 1. NEVER commit .env.production to git (it's in .gitignore)
# 2. For Supabase Edge Functions, set these in Supabase dashboard:
#    - SUPABASE_URL (same as above)
#    - SUPABASE_SERVICE_ROLE_KEY (from Supabase dashboard)
#    - RESEND_API_KEY (from Resend dashboard)
#    - FROM_EMAIL (verified sender domain)
#    - ALLOWED_ORIGIN (your VITE_APP_URL)
# 3. All VITE_ prefixed variables are available in the browser
# 4. Never put sensitive keys in VITE_ variables