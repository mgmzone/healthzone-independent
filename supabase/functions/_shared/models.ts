// Centralized Claude model IDs for all edge functions.
// Version aliases (no date suffix) track the latest snapshot within a version,
// so minor improvements are picked up automatically without surprise major-version bumps.
// Bump these intentionally when a new major version is validated.

export const MODEL_BASIC = "claude-haiku-4-5";
export const MODEL_COACH = "claude-sonnet-4-6";
